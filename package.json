{
  "name": "testing-three",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check-repo-name": "node src/utils/check-repo-name.js",
    "deploy": "npm run check-repo-name && npm run build && git add dist -f && git commit -m deployed-from-script && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "three": "^0.157.0"
  }
}